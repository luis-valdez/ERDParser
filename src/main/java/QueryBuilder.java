import java.util.ArrayList;

public class QueryBuilder {
    Entity entity;
    ArrayList<Relation> relationArrayList;

    public String createTable(Entity entity){
        StringBuilder sb = new StringBuilder( String.format("CREATE TABLE (") );
        sb.append("CREATE TABLE (");
        return "";
    }
}
